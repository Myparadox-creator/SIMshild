{
  "name": "simshield-risk-service",
  "version": "1.0.0",
  "description": "Server-owned mobile identity fraud risk and SIM swap detection engine.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test",
    "db:migrate": "node db/migrate.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "optionalDependencies": {
    "pg": "^8.13.1"
  }
}
